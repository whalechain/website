{
  "name": "@icecreamswap/database",
  "version": "1.0.0",
  "description": "IceCreamSwap database",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "private": true,
  "scripts": {
    "postinstall": "pnpm db:generate",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1"
  },
  "devDependencies": {
    "prisma": "^4.10.1"
  }
}
